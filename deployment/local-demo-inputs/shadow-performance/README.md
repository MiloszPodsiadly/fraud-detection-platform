These records are synthetic local demo inputs only.

They are not production data.
They are not current runtime data.
They are not exported from real transactions.

They exist only to exercise the local/offline FDP-109 generation job.

The generated summary must not expose transaction references or evaluation record IDs.
